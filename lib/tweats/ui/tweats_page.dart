import 'package:flutter/material.dart';

class TweatsPage extends StatelessWidget{
  const TweatsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('tweats');
  }

}
